#!/bin/zsh

# Jira integration MCP stub based on earlier repo configuration.
# This script can be extended to proxy or handle Jira integration requests.

trap 'exit 0' INT TERM

while IFS= read -r line; do
  :
done
